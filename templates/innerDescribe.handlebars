describe('{{description}}', function() {
    var configData=null;
    before(function(done) {
      config.setupMethod()
          .then(function(data) {
              configData = data;
              done();
          });
    });

  {{#each tests}}
  {{this}}
{{/each}}
  });
